import React, {Component} from 'react';

const Header = () => {
  return (
    <header className="header">
      <h1>MineSwepper</h1>
      <button>Start</button>
    </header>
  )
}

export default Header;
